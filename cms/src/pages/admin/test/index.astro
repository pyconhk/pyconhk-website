---
export const prerender = true;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex,nofollow" />
    <title>PyCon HK CMS Test</title>
    <link rel="icon" href="/favicon/favicon.ico" sizes="any" />
    <link
      rel="cms-config-url"
      type="text/yaml"
      href="/admin/config.yml?backend=test"
    />
  </head>
  <body>
    <script>
      import CMS from "decap-cms-app";
      import { validateEditorialPublish } from "../../../lib/editor-validation";

      CMS.registerEventListener({ name: "prePublish", handler: ({ entry }) => validateEditorialPublish(entry.toJS()) });
      CMS.init();
    </script>
  </body>
</html>
