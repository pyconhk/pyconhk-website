---
import '@/styles/global.css';
import { ClientRouter } from 'astro:transitions';
import {
  currentConferenceYear,
  defaultOpenGraphDescription,
  defaultOpenGraphTitle,
  defaultSeoDescription,
  defaultSeoTitle,
  type SiteYear,
  siteName,
  socialHandle,
} from '@/config/site';
import { buildSeoMetadata, toOpenGraphLocale } from '@/lib/seo';
import ImageLoading from './ImageLoading.astro';

interface Props {
  year?: SiteYear;
  title?: string;
  description?: string;
  htmlLang?: string;
  imagePath?: string;
  iconPath?: string;
  robots?: string;
}

const {
  year = currentConferenceYear,
  title = defaultSeoTitle,
  description = defaultSeoDescription,
  htmlLang = 'en-HK',
  imagePath,
  iconPath = '/favicon.ico',
  robots,
} = Astro.props;
const seo = buildSeoMetadata({
  pathname: Astro.url.pathname,
  imagePath,
});
const openGraphTitle = title || defaultOpenGraphTitle;
const openGraphDescription = description || defaultOpenGraphDescription;
const googleAnalyticsId = process.env.PUBLIC_GA_ID || process.env.NEXT_PUBLIC_GA_ID;
const isTestEnvironment =
  process.env.PUBLIC_IS_TEST_ENV === 'true' ||
  process.env.NEXT_PUBLIC_IS_TEST_ENV === 'true';
const shouldLoadGoogleAnalytics = Boolean(googleAnalyticsId && !isTestEnvironment);
---

<!doctype html>
<html lang={htmlLang} data-site-year={year} transition:animate="none">
  <head>
    <ImageLoading />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <slot name="head" />
    <meta name="description" content={description} />
    {robots && <meta name="robots" content={robots} />}
    <title>{title}</title>
    <link rel="canonical" href={seo.canonicalUrl} />
    {seo.alternates.map((alternate) => (
      <link rel="alternate" hreflang={alternate.hreflang} href={alternate.href} />
    ))}
    <meta property="og:type" content="website" />
    <meta property="og:title" content={openGraphTitle} />
    <meta property="og:description" content={openGraphDescription} />
    <meta property="og:url" content={seo.canonicalUrl} />
    <meta property="og:site_name" content={siteName} />
    <meta property="og:locale" content={toOpenGraphLocale(htmlLang)} />
    {seo.alternates.filter((alternate) => alternate.hreflang !== 'x-default').map((alternate) => (
      <meta property="og:locale:alternate" content={toOpenGraphLocale(alternate.hreflang)} />
    ))}
    <meta property="og:image" content={seo.imageUrl} />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={openGraphTitle} />
    <meta name="twitter:description" content={openGraphDescription} />
    <meta name="twitter:creator" content={socialHandle} />
    <meta name="twitter:site" content={socialHandle} />
    <meta name="twitter:image" content={seo.imageUrl} />
    <link rel="icon" href={iconPath} />
    <ClientRouter fallback="swap" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="" />
    <link
      href={year === 2025
        ? 'https://fonts.googleapis.com/css2?family=Lexend:wght@400;600;700&family=Montserrat:wght@400;500;600;700&display=swap'
        : year === 2026
        ? 'https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,300..900;1,300..900&display=swap'
        : 'https://fonts.googleapis.com/css2?family=Besley:ital,wght@0,400..900;1,400..900&family=Bodoni+Moda:ital,opsz,wght@0,6..96,400..900;1,6..96,400..900&family=Fraunces:ital,opsz,wght@0,9..144,300..900;1,9..144,300..900&family=Lora:ital,wght@0,400..700;1,400..700&family=Merriweather:ital,wght@0,700;0,900;1,700;1,900&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Roboto:ital,wght@0,300..900;1,300..900&display=swap'}
      rel="stylesheet"
    />
    {
      shouldLoadGoogleAnalytics && (
        <>
          <script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${googleAnalyticsId}`} />
          <script is:inline define:vars={{ googleAnalyticsId }}>
            window.dataLayer = window.dataLayer || []
            function gtag(...args) {
              window.dataLayer.push(args)
            }
            gtag('js', new Date())
            gtag('config', googleAnalyticsId)
          </script>
        </>
      )
    }
  </head>
  <body class="min-h-screen overflow-x-clip bg-white font-sans text-slate-700 antialiased">
    <div class="min-h-screen overflow-x-clip">
      <slot />
    </div>
  </body>
</html>
