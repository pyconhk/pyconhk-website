---
import {
  resolveCfpLocale,
  supportedLocales,
} from '@/years/2026/data/cfp';
import CfpLandingPage from '@/years/2026/routes/CfpLandingPage.astro';

export function getStaticPaths() {
  return supportedLocales.map((locale) => ({
    params: { locale },
  }));
}

const locale = resolveCfpLocale(Astro.params.locale);
---

<CfpLandingPage locale={locale} />
