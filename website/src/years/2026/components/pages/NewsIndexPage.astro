---
import { getPublishedPosts } from '@/lib/news';
import NewsCard from '@/years/2026/components/news/NewsCard.astro';
import { newsMessages } from '@/years/2026/i18n/news';
import ConferenceLayout from '@/years/2026/layouts/ConferenceLayout.astro';
import type { CfpLocale } from '@/years/2026/locales';

interface Props {
  locale: CfpLocale;
}

const { locale } = Astro.props;
const articles = await getPublishedPosts(2026, locale);
const copy = newsMessages[locale];
---

<ConferenceLayout title={`${copy.title} | PyCon HK 2026`} description={copy.description} locale={locale}>
  <section class="w-full bg-conference-paper px-4 py-10 sm:px-6 sm:py-16 lg:px-8">
    <div class="mx-auto max-w-6xl rounded-3xl border-3 border-conference-outline bg-conference-surface p-6 shadow-conference-panel sm:p-10 md:p-14">
      <header class="mx-auto mb-10 max-w-2xl space-y-3 text-center sm:mb-12">
        <p class="inline-flex rounded-full border-2 border-conference-outline bg-conference-paper px-3.5 py-1 text-xs font-black uppercase text-conference-ink shadow-conference-control">{copy.subtitle}</p>
        <h1 class="font-sans text-3xl font-black uppercase tracking-tight text-conference-ink sm:text-4xl md:text-5xl">{copy.title}</h1>
        <p class="text-sm font-medium text-conference-body sm:text-base">{copy.description}</p>
      </header>
      {articles.length > 0 ? (
        <div class="grid grid-cols-1 gap-6 sm:gap-8 md:grid-cols-2 lg:grid-cols-3">
          {articles.map(article => <NewsCard article={article} locale={locale} />)}
        </div>
      ) : (
        <p class="rounded-2xl border-2 border-conference-outline/15 bg-conference-paper p-8 text-center text-conference-body">{copy.empty}</p>
      )}
    </div>
  </section>
</ConferenceLayout>
